# Running tests

The tests are contained in the `tests.py`, usable with Python's own [`unittest`](https://docs.python.org/3/library/unittest.html#module-unittest).

Run tests with e.g.:

```console
poetry run -P /path/to/tsd-file-api python -m unittest
```
